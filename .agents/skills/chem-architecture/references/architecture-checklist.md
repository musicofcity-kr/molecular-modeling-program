# Architecture Checklist

- Is the editor separated from validation?
- Is the molecule data model explicit?
- Can invalid input be represented safely?
- Is 3D visualization optional and non-blocking?
- Is the dependency decision logged?
- Can the MVP run without a backend?
- Does every calculated result trace back to a validated structure?
