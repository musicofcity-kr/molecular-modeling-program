# Scripts

- `install_to_project.ps1`: Windows PowerShell installer for copying skills into a target Codex project folder.
- `verify_skill_package.py`: Checks `SKILL.md` files for required front matter.
